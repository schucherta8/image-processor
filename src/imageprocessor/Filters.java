package imageprocessor;

/**
 * The Filters enum class specifies the different filters of an ImageModelImpl that can be
 * applied to an image.
 */
public enum Filters {
  BLUR,SHARPEN,SEPIA,GREYSCALE,DITHER
}
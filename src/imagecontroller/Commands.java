package imagecontroller;

/**
 * The Commands enum represents the available set of commands in an ImageProcessor implementation
 * that may retrieved by the Controller, and which the Controller may interpret into a
 * command for the Model.
 */
enum Commands {
  LOAD,APPLY,GENERATE,SAVE
}
